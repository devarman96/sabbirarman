# Admin Credentials
ADMIN_PASSWORD=your_secure_password_here

# Firebase Config (already internal, but good to have placeholders if needed)
# GEMINI_API_KEY=
# SMTP_USER=
# SMTP_PASS=
# RECEIVER_EMAIL=
